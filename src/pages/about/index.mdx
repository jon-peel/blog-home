import { graphql } from 'gatsby';
import Img from 'gatsby-image';
import PageTemplate from 'templates/PageTemplate';

export default PageTemplate;

export const pageQuery = graphql`
	query About {
		head: file(relativePath: { eq: "about/head.jpg" }) {
			image: childImageSharp {
				fluid(maxWidth: 180, quality: 50) {
					...GatsbyImageSharpFluid
				}
			}
		}
	}
`;

## About


I am a software developer, working fulltime since 2004, working mostly in C#.  
My current work is mostly in .Net Framework, but do have a few smaller .Net Core projects.

My prefered frontend framework is React, with Typescript.

I enjoy playing around with F#, but I havent yet found a way of incorporating that into work more.

<Img fluid={props.data.head.image.fluid} style={{width: 180, height: 180 * props.data.head.image.fluid.aspectRatio}} />
